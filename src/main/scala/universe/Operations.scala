package universe

// TODO: syntactic sugar traits to declare ops + inductive types to classify ops in arity, constructor/modifier/query, etc.

trait Operations {
  //  self: Individuals with Particulars with Universals with Morphisms =>
  //
  //  abstract class Constant[X <: Particular, U <: Universal](implicit u: U) {
  //    implicit val me: this.type
  //    implicit object asMorphism extends (this.type :: U)
  //  }
  //
  //  trait op1[X <: Particular, A <: Universal, B <: Universal]
  //  trait op2[A <: Universal, B <: Universal] extends Function1[A,B]

}
